#include <raylib.h>
#include <cstdlib>

int main(void)
{
    int windowWidth = 750;
    int windowHeight = 700;

    InitWindow(windowWidth, windowHeight, "C++ Space Invaders");

    CloseWindow();
    exit(EXIT_SUCCESS);
}